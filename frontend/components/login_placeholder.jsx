const React = require('react');
const LoginPlaceholder = React.createClass({
  render(){
    return(
      <h1>You need to be logged in to see this page!</h1>
    );
  }
});

module.exports = LoginPlaceholder;
